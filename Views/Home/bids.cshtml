@model Bid
@* @{
    var user = await UserManager.GetUserAsync(User);
    var userId = user?.Id;
} *@

<form asp-action="Bid" asp-route-id=1 class="form" method="POST">
    <div class="form=group">
        <input asp-for="UserName" value=1 hidden />
        <input asp-for="ListingId" value=1 hidden />
    </div>
    <div class="input-group form-group my-3">
        <span class="input-group-text" id="basic-addon1">$</span>
        <input type="number" asp-for="Amount" class="form-control" placeholder="Bid" aria-describedby="basic-addon1" required>
    </div>
    <div class="form-group my-3">
        <button type="submit" class="btn btn-secondary">Place Bid</button>
    </div>
</form>